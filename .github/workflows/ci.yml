name: CI

on:
  workflow_dispatch:

# Uncomment to enable automatic CI on push/PR
# on:
#   push:
#     branches: [main]
#   pull_request:
#     branches: [main]

env:
  TF_VERSION: "1.7.5"
  AWS_ACCESS_KEY_ID: test
  AWS_SECRET_ACCESS_KEY: test
  AWS_DEFAULT_REGION: us-east-1

jobs:
  fmt:
    name: Format
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: ${{ env.TF_VERSION }}
      - run: terraform fmt -check -recursive

  validate:
    name: Validate
    runs-on: ubuntu-latest
    needs: fmt
    steps:
      - uses: actions/checkout@v4
      - uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: ${{ env.TF_VERSION }}
      - name: Init (dry run — no backend)
        run: terraform init -backend=false
      - name: Validate
        run: terraform validate

  security:
    name: Security Scan
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: aquasecurity/trivy-action@master
        with:
          scan-type: fs
          scan-ref: .
          format: table
          severity: CRITICAL,HIGH
          exit-code: "1"

# ── Plan dry run not available for this repo ────────────────────────────────
# data.tf uses data "terraform_remote_state" "eks" which requires
# a real S3 backend + deployed EKS state. Cannot plan without real AWS.
#
# ── COMMENTED OUT — activate once AWS OIDC role + state bucket are provisioned ─
#
# plan:
#   name: Plan (real — posts comment on PR)
#   runs-on: ubuntu-latest
#   needs: validate
#   if: github.event_name == 'pull_request'
#   permissions:
#     id-token: write
#     contents: read
#     pull-requests: write
#   strategy:
#     matrix:
#       env: [dev, staging]
#   steps:
#     - uses: actions/checkout@v4
#     - uses: hashicorp/setup-terraform@v3
#       with:
#         terraform_version: ${{ env.TF_VERSION }}
#     - uses: aws-actions/configure-aws-credentials@v4
#       with:
#         role-to-assume: ${{ secrets.AWS_ROLE_ARN }}
#         aws-region: us-east-1
#     - run: |
#         terraform init \
#           -backend-config="bucket=${{ secrets.TF_STATE_BUCKET }}" \
#           -backend-config="key=${{ matrix.env }}/aj-infra-platform/terraform.tfstate" \
#           -backend-config="region=us-east-1"
#     - run: terraform plan -var-file=envs/${{ matrix.env }}.tfvars -no-color 2>&1 | tee plan.txt
#     - uses: actions/github-script@v7
#       with:
#         script: |
#           const plan = require('fs').readFileSync('plan.txt','utf8').substring(0,65000);
#           github.rest.issues.createComment({
#             issue_number: context.issue.number,
#             owner: context.repo.owner, repo: context.repo.repo,
#             body: `### Terraform Plan — \`${{ matrix.env }}\`\n\`\`\`\n${plan}\n\`\`\``
#           });
